import { FilterStreamPipe } from './filter-stream.pipe';

describe('FilterStreamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStreamPipe();
    expect(pipe).toBeTruthy();
  });
});
